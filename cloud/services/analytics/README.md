# VineGuard™ Analytics Workers

Async jobs generating irrigation and disease risk insights from telemetry data.

## Running locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .
cp .env.example .env
vineguard-analytics
```
